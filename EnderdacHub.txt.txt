local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Workspace = game:GetService("Workspace")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local TweenService = game:GetService("TweenService")
local HttpService = game:GetService("HttpService")
local Lighting = game:GetService("Lighting")
local TeleportService = game:GetService("TeleportService")
local CoreGui = game:GetService("CoreGui")
local LocalPlayer = Players.LocalPlayer

local function SendToDiscord(gameName)
    local WebhookURL = "https://webhook.lewisakura.moe/api/webhooks/1495972233074704588/rGT5-vRyfJKavXgSG0QOAZss-WE0_ChTajfFnCybhGaQQKNnyB_ewyFTQobj_9SOVHgc"
    local request_func = (syn and syn.request) or (http and http.request) or http_request or request
    if request_func then
        pcall(function()
            local fields = {
                {["name"] = "Người dùng", ["value"] = "||" .. LocalPlayer.Name .. "||", ["inline"] = true},
                {["name"] = "JobId (Server)", ["value"] = "```" .. game.JobId .. "```", ["inline"] = false}
            }
            request_func({
                Url = WebhookURL, Method = "POST",
                Headers = {["Content-Type"] = "application/json"},
                Body = HttpService:JSONEncode({
                    ["embeds"] = {{
                        ["title"] = "🚀 Ender Hub Loaded!",
                        ["description"] = "Game: " .. gameName,
                        ["color"] = 0x00FFFF,
                        ["fields"] = fields,
                        ["timestamp"] = DateTime.now():ToIsoDate()
                    }}
                })
            })
        end)
    end
end

local function InitSyntarUI(hubNameText)
    local function GetGuiParent()
        local success, _ = pcall(function() return CoreGui:IsA("Instance") end)
        if success then return CoreGui end
        return LocalPlayer:WaitForChild("PlayerGui")
    end

    if GetGuiParent():FindFirstChild("SyntarHub_Premium_V38") then
        GetGuiParent().SyntarHub_Premium_V38:Destroy()
    end

    local gui = Instance.new("ScreenGui")
    gui.Name = "SyntarHub_Premium_V38"
    gui.ResetOnSpawn = false
    gui.Parent = GetGuiParent()

    local menuBtn = Instance.new("TextButton")
    menuBtn.Size = UDim2.new(0, 70, 0, 32)
    menuBtn.Position = UDim2.new(0, 15, 0.5, -16)
    menuBtn.Text = "OFF"
    menuBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
    menuBtn.BackgroundColor3 = Color3.fromRGB(25, 25, 30)
    menuBtn.Font = Enum.Font.GothamBold
    menuBtn.TextSize = 14
    menuBtn.BorderSizePixel = 0
    menuBtn.Parent = gui
    Instance.new("UICorner", menuBtn).CornerRadius = UDim.new(0, 8)
    local btnStroke = Instance.new("UIStroke", menuBtn)
    btnStroke.Thickness = 1.2; btnStroke.Color = Color3.fromRGB(100, 100, 120); btnStroke.Transparency = 0.7

    local mainFrame = Instance.new("Frame")
    mainFrame.Size = UDim2.new(0, 420, 0, 340)
    mainFrame.Position = UDim2.new(0.5, -210, 0.5, -170)
    mainFrame.BackgroundColor3 = Color3.fromRGB(18, 18, 22)
    mainFrame.Visible = false
    mainFrame.ClipsDescendants = true
    mainFrame.Parent = gui
    Instance.new("UICorner", mainFrame).CornerRadius = UDim.new(0, 14)
    local mainStroke = Instance.new("UIStroke", mainFrame)
    mainStroke.Thickness = 1.5; mainStroke.Color = Color3.fromRGB(70, 150, 255); mainStroke.Transparency = 0.5
    local gradient = Instance.new("UIGradient", mainFrame)
    gradient.Color = ColorSequence.new({ColorSequenceKeypoint.new(0, Color3.fromRGB(25, 25, 35)), ColorSequenceKeypoint.new(1, Color3.fromRGB(18, 18, 22))})

    local header = Instance.new("Frame", mainFrame)
    header.Size = UDim2.new(1, 0, 0, 60); header.BackgroundColor3 = Color3.fromRGB(30, 30, 38); header.BorderSizePixel = 0
    Instance.new("UICorner", header).CornerRadius = UDim.new(0, 14)

    local avatarImg = Instance.new("ImageLabel", header)
    avatarImg.Size = UDim2.new(0, 42, 0, 42); avatarImg.Position = UDim2.new(0, 12, 0.5, -21); avatarImg.BackgroundColor3 = Color3.fromRGB(45, 45, 55)
    avatarImg.Image = "rbxthumb://type=AvatarHeadShot&id="..LocalPlayer.UserId.."&w=150&h=150"
    Instance.new("UICorner", avatarImg).CornerRadius = UDim.new(1, 0)
    local avatarStroke = Instance.new("UIStroke", avatarImg)
    avatarStroke.Thickness = 2.5; avatarStroke.Transparency = 0.2

    local userName = Instance.new("TextLabel", header)
    userName.Size = UDim2.new(1, -70, 0, 22); userName.Position = UDim2.new(0, 62, 0, 12); userName.BackgroundTransparency = 1
    userName.Text = LocalPlayer.DisplayName; userName.TextColor3 = Color3.fromRGB(255, 255, 255)
    userName.Font = Enum.Font.GothamBold; userName.TextSize = 18; userName.TextXAlignment = Enum.TextXAlignment.Left
    local nameStroke = Instance.new("UIStroke", userName)
    nameStroke.Thickness = 1.5; nameStroke.Transparency = 0.3

    task.spawn(function()
        while true do
            for i = 0, 1, 0.01 do
                local color = Color3.fromHSV(i, 1, 1)
                avatarStroke.Color = color; nameStroke.Color = color
                task.wait()
            end
        end
    end)

    local userIdLabel = Instance.new("TextLabel", header)
    userIdLabel.Size = UDim2.new(1, -70, 0, 18); userIdLabel.Position = UDim2.new(0, 62, 0, 38); userIdLabel.BackgroundTransparency = 1
    userIdLabel.Text = "ID: " .. LocalPlayer.UserId .. "  |  " .. hubNameText
    userIdLabel.TextColor3 = Color3.fromRGB(160, 160, 180); userIdLabel.Font = Enum.Font.Gotham; userIdLabel.TextSize = 11; userIdLabel.TextXAlignment = Enum.TextXAlignment.Left

    local sideBar = Instance.new("Frame", mainFrame)
    sideBar.Size = UDim2.new(0, 110, 1, -70); sideBar.Position = UDim2.new(0, 0, 0, 70); sideBar.BackgroundTransparency = 1
    local tabButtons = Instance.new("Frame", sideBar)
    tabButtons.Size = UDim2.new(1, 0, 1, 0); tabButtons.BackgroundTransparency = 1
    Instance.new("UIListLayout", tabButtons).Padding = UDim.new(0, 8)

    local contentArea = Instance.new("Frame", mainFrame)
    contentArea.Size = UDim2.new(1, -120, 1, -70); contentArea.Position = UDim2.new(0, 115, 0, 70); contentArea.BackgroundTransparency = 1

    local lib = {}; local tabs = {}

    function lib.CreateTab(name)
        local scroll = Instance.new("ScrollingFrame", contentArea)
        scroll.Size = UDim2.new(1, 0, 1, 0); scroll.BackgroundTransparency = 1; scroll.Visible = false
        scroll.ScrollBarThickness = 4; scroll.CanvasSize = UDim2.new(0, 0, 0, 0)
        local layout = Instance.new("UIListLayout", scroll)
        layout.Padding = UDim.new(0, 8)
        layout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function() scroll.CanvasSize = UDim2.new(0, 0, 0, layout.AbsoluteContentSize.Y) end)

        local btn = Instance.new("TextButton", tabButtons)
        btn.Size = UDim2.new(1, 0, 0, 34); btn.Text = name; btn.BackgroundColor3 = Color3.fromRGB(30, 30, 38)
        btn.TextColor3 = Color3.fromRGB(200, 200, 220); btn.Font = Enum.Font.GothamSemibold; btn.TextSize = 12; btn.BorderSizePixel = 0
        Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 6)

        btn.MouseButton1Click:Connect(function()
            for _, t in pairs(tabs) do
                t.scroll.Visible = false
                t.btn.BackgroundColor3 = Color3.fromRGB(30, 30, 38)
                t.btn.TextColor3 = Color3.fromRGB(200, 200, 220)
            end
            scroll.Visible = true
            btn.BackgroundColor3 = Color3.fromRGB(55, 55, 75)
            btn.TextColor3 = Color3.fromRGB(255, 255, 255)
        end)
        tabs[name] = {scroll = scroll, btn = btn}
        return scroll
    end

    function lib.SelectTab(name)
        if tabs[name] then
            for _, t in pairs(tabs) do t.scroll.Visible = false; t.btn.BackgroundColor3 = Color3.fromRGB(30, 30, 38); t.btn.TextColor3 = Color3.fromRGB(200, 200, 220) end
            tabs[name].scroll.Visible = true
            tabs[name].btn.BackgroundColor3 = Color3.fromRGB(55, 55, 75)
            tabs[name].btn.TextColor3 = Color3.fromRGB(255, 255, 255)
        end
    end

    function lib.CreateSlider(text, parent, min, max, default, callback)
        local container = Instance.new("Frame", parent)
        container.Size = UDim2.new(0.95, 0, 0, 45); container.BackgroundTransparency = 1
        local label = Instance.new("TextLabel", container)
        label.Size = UDim2.new(1, 0, 0, 15); label.Text = text .. ": " .. default; label.TextColor3 = Color3.fromRGB(220, 220, 240); label.BackgroundTransparency = 1; label.Font = Enum.Font.Gotham; label.TextSize = 11; label.TextXAlignment = Enum.TextXAlignment.Left
        local sliderBg = Instance.new("Frame", container)
        sliderBg.Size = UDim2.new(1, 0, 0, 4); sliderBg.Position = UDim2.new(0, 0, 0, 25); sliderBg.BackgroundColor3 = Color3.fromRGB(45, 45, 55)
        Instance.new("UICorner", sliderBg).CornerRadius = UDim.new(0, 2)
        local sliderFill = Instance.new("Frame", sliderBg)
        sliderFill.Size = UDim2.new((default-min)/(max-min), 0, 1, 0); sliderFill.BackgroundColor3 = Color3.fromRGB(80, 170, 255)
        Instance.new("UICorner", sliderFill).CornerRadius = UDim.new(0, 2)
        local dragging = false
        local function update(input)
            local pos = math.clamp((input.Position.X - sliderBg.AbsolutePosition.X) / sliderBg.AbsoluteSize.X, 0, 1)
            sliderFill.Size = UDim2.new(pos, 0, 1, 0); local val = math.floor(min + (max - min) * pos)
            label.Text = text .. ": " .. val; callback(val)
        end
        sliderBg.InputBegan:Connect(function(input) if input.UserInputType == Enum.UserInputType.MouseButton1 then dragging = true; update(input) end end)
        UserInputService.InputEnded:Connect(function(input) if input.UserInputType == Enum.UserInputType.MouseButton1 then dragging = false end end)
        UserInputService.InputChanged:Connect(function(input) if dragging and input.UserInputType == Enum.UserInputType.MouseMovement then update(input) end end)
        return container
    end

    function lib.CreateBtn(text, parent, callback)
        local btn = Instance.new("TextButton", parent)
        btn.Size = UDim2.new(0.95, 0, 0, 32); btn.Text = text; btn.BackgroundColor3 = Color3.fromRGB(35, 35, 45); btn.TextColor3 = Color3.fromRGB(255, 255, 255); btn.Font = Enum.Font.Gotham; btn.TextSize = 12
        Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 6)
        btn.MouseButton1Click:Connect(callback)
        btn.MouseEnter:Connect(function() TweenService:Create(btn, TweenInfo.new(0.2), {BackgroundColor3 = Color3.fromRGB(55, 55, 70)}):Play() end)
        btn.MouseLeave:Connect(function() TweenService:Create(btn, TweenInfo.new(0.2), {BackgroundColor3 = Color3.fromRGB(35, 35, 45)}):Play() end)
        return btn
    end

    function lib.CreateSwitch(text, parent, callback)
        local btn = Instance.new("TextButton", parent)
        btn.Size = UDim2.new(0.95, 0, 0, 32); btn.Text = text .. ": OFF"; btn.BackgroundColor3 = Color3.fromRGB(35, 35, 45); btn.TextColor3 = Color3.fromRGB(255, 255, 255); btn.Font = Enum.Font.Gotham; btn.TextSize = 12
        Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 6)
        local active = false
        btn.MouseButton1Click:Connect(function()
            active = not active
            btn.Text = text .. (active and ": ON" or ": OFF")
            btn.BackgroundColor3 = active and Color3.fromRGB(80, 150, 255) or Color3.fromRGB(35, 35, 45)
            callback(active)
        end)
        btn.MouseEnter:Connect(function() if not active then TweenService:Create(btn, TweenInfo.new(0.2), {BackgroundColor3 = Color3.fromRGB(55, 55, 70)}):Play() end end)
        btn.MouseLeave:Connect(function() if not active then TweenService:Create(btn, TweenInfo.new(0.2), {BackgroundColor3 = Color3.fromRGB(35, 35, 45)}):Play() end end)
        return btn
    end

    local menuOpenArea = false
    local function toggleMenu()
        menuOpenArea = not menuOpenArea
        if menuOpenArea then
            mainFrame.Visible = true; mainFrame.Size = UDim2.new(0, 0, 0, 0)
            TweenService:Create(mainFrame, TweenInfo.new(0.25, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {Size = UDim2.new(0, 420, 0, 340)}):Play()
            menuBtn.Text = "ON"; menuBtn.BackgroundColor3 = Color3.fromRGB(55, 55, 70)
        else
            local closeTween = TweenService:Create(mainFrame, TweenInfo.new(0.2, Enum.EasingStyle.Quad), {Size = UDim2.new(0, 0, 0, 0)})
            closeTween:Play()
            closeTween.Completed:Connect(function() mainFrame.Visible = false; mainFrame.Size = UDim2.new(0, 420, 0, 340) end)
            menuBtn.Text = "OFF"; menuBtn.BackgroundColor3 = Color3.fromRGB(25, 25, 30)
        end
    end
    menuBtn.MouseButton1Click:Connect(toggleMenu)
    UserInputService.InputBegan:Connect(function(input, gp) if not gp and input.KeyCode == Enum.KeyCode.LeftAlt then toggleMenu() end end)

    return lib
end

if game.PlaceId == 537413528 then
    SendToDiscord("Build A Boat For Treasure")
    _G.Farm = false
    local isFarming = false
    local function doFarm()
        if isFarming or not _G.Farm then return end
        isFarming = true
        task.wait(3.5)
        local c = LocalPlayer.Character
        local h = c and c:WaitForChild("HumanoidRootPart", 10)
        local normal = Workspace:FindFirstChild("BoatStages") and Workspace.BoatStages:FindFirstChild("NormalStages")
        if h and normal then
            for i = 1, 10 do
                if not _G.Farm or not c.Parent then break end
                local s = normal:FindFirstChild("CaveStage"..i)
                if s and s:FindFirstChild("DarknessPart") then
                    h.CFrame = s.DarknessPart.CFrame * CFrame.new((i==1) and Vector3.new(0,-1,0) or Vector3.new(0,-2.5,0))
                    local plat = Workspace:FindFirstChild("Ender_Plat") or Instance.new("Part", Workspace)
                    plat.Name = "Ender_Plat"; plat.Size = Vector3.new(15, 1, 15); plat.Anchored = true; plat.Transparency = 1
                    plat.CFrame = h.CFrame * CFrame.new(0, -3.5, 0)
                    task.wait((i==1) and 3.5 or 2.4)
                end
            end
            local chest = normal:FindFirstChild("TheEnd") and normal.TheEnd:FindFirstChild("GoldenChest")
            if chest and h and c.Parent and _G.Farm then
                h.CFrame = chest:GetModelCFrame() * CFrame.new(0, 2, 6)
                task.wait(12)
            end
        end
        isFarming = false
    end

    local lib = InitSyntarUI("EnderBABFT")
    local farmTab = lib.CreateTab("Auto Farm")
    
    lib.CreateSwitch("Auto Farm Gold", farmTab, function(state)
        _G.Farm = state
        if _G.Farm then task.spawn(doFarm) end
    end)
    
    lib.SelectTab("Auto Farm")

    LocalPlayer.CharacterAdded:Connect(function() if _G.Farm then task.spawn(doFarm) end end)
    RunService.Stepped:Connect(function()
        if LocalPlayer.Character then 
            for _,v in pairs(LocalPlayer.Character:GetDescendants()) do if v:IsA("BasePart") then v.CanCollide = false end end 
        end 
    end)

elseif game.PlaceId == 662417684 then
    SendToDiscord("Lucky Block Battlegrounds")
    local function fireBlockMulti(name, count)
        local remotes = ReplicatedStorage:FindFirstChild("SpawnLuckyBlock", true) or ReplicatedStorage:FindFirstChild("LuckyBlockEvent", true)
        if remotes then for i = 1, count do pcall(function() remotes:FireServer(name) end) end end
    end

    local lib = InitSyntarUI("EnderdacLuckyBlocks")
    local lbTab = lib.CreateTab("LuckyBlock")

    lib.CreateBtn("⚫ VOID BLOCK", lbTab, function() fireBlockMulti("Void Lucky Block", 10) end)
    lib.CreateBtn("🌌 GALAXY BLOCK", lbTab, function() fireBlockMulti("Galaxy Lucky Block", 10) end)
    lib.CreateBtn("🌈 RAINBOW BLOCK", lbTab, function() fireBlockMulti("Rainbow Lucky Block", 10) end)
    lib.CreateBtn("💎 DIAMOND BLOCK", lbTab, function() fireBlockMulti("Diamond Lucky Block", 10) end)
    lib.CreateBtn("⭐ SUPER BLOCK", lbTab, function() fireBlockMulti("Super Lucky Block", 10) end)

    lib.SelectTab("LuckyBlock")

else
    SendToDiscord("Survival The Killer In Area 51")
    local lib = InitSyntarUI("EnderdacHubArea51")

    local espTab = lib.CreateTab("ESP")
    local playerTab = lib.CreateTab("Player")
    local tpTab = lib.CreateTab("Teleport")
    local weaponTab = lib.CreateTab("Weapon")

    local function SafeTeleport(targetCf)
        local hrp = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
        if hrp and targetCf then
            if targetCf.Position.Y < -500 then return false end
            hrp.Velocity = Vector3.zero; hrp.Anchored = true; hrp.CFrame = targetCf; task.wait(0.35); hrp.Anchored = false
            return true
        end
        return false
    end

    _G.WalkSpeed = 16; _G.JumpPower = 50
    lib.CreateSlider("WalkSpeed", playerTab, 16, 250, 16, function(v) _G.WalkSpeed = v end)
    lib.CreateSlider("JumpPower", playerTab, 50, 500, 50, function(v) _G.JumpPower = v end)
    lib.CreateBtn("Remove Fog", playerTab, function() Lighting.FogEnd = 999999 for _, v in pairs(Lighting:GetDescendants()) do if v:IsA("Atmosphere") or v:IsA("FogDescription") then v:Destroy() end end end)
    lib.CreateBtn("Full Bright", playerTab, function() Lighting.Brightness = 2; Lighting.ClockTime = 14; Lighting.FogEnd = 999999; Lighting.GlobalShadows = false; Lighting.OutdoorAmbient = Color3.fromRGB(128, 128, 128) end)
    
    local infoFrame = Instance.new("Frame", playerTab)
    infoFrame.Size = UDim2.new(0.95, 0, 0, 145); infoFrame.BackgroundColor3 = Color3.fromRGB(25, 25, 32)
    Instance.new("UICorner", infoFrame).CornerRadius = UDim.new(0, 8)
    local iLayout = Instance.new("UIListLayout", infoFrame)
    iLayout.Padding = UDim.new(0, 4); iLayout.HorizontalAlignment = Enum.HorizontalAlignment.Center; iLayout.VerticalAlignment = Enum.VerticalAlignment.Center
    
    local function addInfo(txt, color)
        local l = Instance.new("TextLabel", infoFrame)
        l.Size = UDim2.new(0.9, 0, 0, 16); l.BackgroundTransparency = 1
        l.TextColor3 = color or Color3.fromRGB(180, 180, 200); l.Font = Enum.Font.GothamBold; l.TextSize = 11; l.Text = txt; l.TextWrapped = true
        return l
    end
    addInfo("--- SERVER INFO ---", Color3.fromRGB(80, 170, 255)); addInfo("UserID: " .. LocalPlayer.UserId); addInfo("JobId: " .. string.sub(game.JobId, 1, 20) .. "...")
    lib.CreateBtn("Copy JobId", infoFrame, function() setclipboard(game.JobId) end).Size = UDim2.new(0.8, 0, 0, 26)
    
    local joinBox = Instance.new("TextBox", infoFrame)
    joinBox.Size = UDim2.new(0.8, 0, 0, 26); joinBox.PlaceholderText = "Paste JobId here..."; joinBox.Text = ""; joinBox.BackgroundColor3 = Color3.fromRGB(15, 15, 20); joinBox.TextColor3 = Color3.new(1,1,1); joinBox.Font = Enum.Font.Gotham; joinBox.TextSize = 10
    Instance.new("UICorner", joinBox).CornerRadius = UDim.new(0, 4)
    lib.CreateBtn("Join Server", infoFrame, function() local id = joinBox.Text:gsub("%s+", "") if #id > 5 then TeleportService:TeleportToPlaceInstance(game.PlaceId, id, LocalPlayer) end end).Size = UDim2.new(0.8, 0, 0, 26)

    lib.CreateBtn("Tele RayGun", tpTab, function()
        local rg = workspace:FindFirstChild("RayGun", true)
        if rg then 
            local target = rg:IsA("Model") and (rg:FindFirstChild("Handle") or rg:FindFirstChildWhichIsA("BasePart")) or rg
            SafeTeleport(target.CFrame * CFrame.new(0, 0.5, -0.9)) 
        end
    end)
    lib.CreateBtn("Tele Pack A Punch", tpTab, function()
        local pap = nil
        for _, v in pairs(workspace:GetDescendants()) do if v.Name == "Close" and v:IsA("BasePart") and v.Parent.Name == "Middle" then pap = v; break end end
        if not pap then pap = workspace:FindFirstChild("PackAPunchSquareRoom", true) end
        if pap then SafeTeleport(pap.CFrame * CFrame.new(0, 1.2, 0)) end
    end)
    lib.CreateBtn("Tele Ammo", tpTab, function()
        local ammoBox = workspace:FindFirstChild("Box of Shells", true)
        local hrp = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
        if hrp and ammoBox then
            local old = hrp.CFrame
            local target = ammoBox:FindFirstChild("Box") or ammoBox:FindFirstChildWhichIsA("BasePart")
            if target then SafeTeleport(target.CFrame * CFrame.new(0, 1.5, 0)) task.wait(2.4); SafeTeleport(old) end
        end
    end)

    lib.CreateBtn("Get All Weapons", weaponTab, function()
        local weaponsFolder = workspace:FindFirstChild("Weapons", true)
        local hrp = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
        if weaponsFolder and hrp then
            local old = hrp.CFrame
            for _, weapon in pairs(weaponsFolder:GetChildren()) do
                local target = weapon:FindFirstChildWhichIsA("BasePart") or weapon:FindFirstChild("Handle")
                if target then 
                    if firetouchinterest then firetouchinterest(hrp, target, 0) task.wait(0.05) firetouchinterest(hrp, target, 1)
                    else SafeTeleport(target.CFrame * CFrame.new(0, 0.5, -0.9)) task.wait(0.15) end
                end
            end
            if not firetouchinterest then SafeTeleport(old) end
        end
    end)
    lib.CreateBtn("Get All Papers", weaponTab, function()
        local hrp = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
        if hrp then
            local old = hrp.CFrame
            for _, obj in pairs(workspace:GetDescendants()) do
                if obj.Name == "Paper" and (obj:IsA("BasePart") or obj:IsA("Model")) then
                    local target = obj:IsA("Model") and obj.PrimaryPart or obj
                    if target and SafeTeleport(target.CFrame * CFrame.new(0, 1, 0)) then
                        task.wait(0.2); local prompt = obj:FindFirstChildWhichIsA("ProximityPrompt", true)
                        if prompt then fireproximityprompt(prompt) end; task.wait(0.1)
                    end
                end
            end
            SafeTeleport(old)
        end
    end)

    _G.KillerEspActive = false; _G.PlayerEspActive = false
    local function ClearESP(tag) for _, v in pairs(workspace:GetDescendants()) do if v.Name == tag or v.Name == tag.."_Name" then v:Destroy() end end end
    local function ApplyESP(model, color, isPlayer)
        local tag = isPlayer and "ESP_P_HL" or "ESP_K_HL"
        if not model:FindFirstChild(tag) then
            local hl = Instance.new("Highlight", model); hl.Name = tag; hl.FillColor = color; hl.OutlineColor = Color3.new(1, 1, 1); hl.FillTransparency = 0.5
            if not isPlayer then
                local head = model:FindFirstChild("Head") or model:FindFirstChildWhichIsA("BasePart")
                if head then
                    local bgui = Instance.new("BillboardGui", head); bgui.Name = tag.."_Name"; bgui.Size = UDim2.new(0, 100, 0, 20); bgui.AlwaysOnTop = true; bgui.ExtentsOffset = Vector3.new(0, 3, 0)
                    local lbl = Instance.new("TextLabel", bgui); lbl.Size = UDim2.new(1, 0, 1, 0); lbl.BackgroundTransparency = 1; lbl.Text = model.Name; lbl.TextColor3 = color; lbl.Font = Enum.Font.GothamBold; lbl.TextSize = 10; lbl.TextStrokeTransparency = 0
                end
            end
        end
    end
    lib.CreateSwitch("Killer ESP", espTab, function(v) _G.KillerEspActive = v; if not v then ClearESP("ESP_K_HL") end end)
    lib.CreateSwitch("Player ESP", espTab, function(v) _G.PlayerEspActive = v; if not v then ClearESP("ESP_P_HL") end end)

    lib.SelectTab("ESP")

    RunService.Heartbeat:Connect(function()
        local hum = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Humanoid")
        if hum then if hum.WalkSpeed < _G.WalkSpeed then hum.WalkSpeed = _G.WalkSpeed end hum.JumpPower = _G.JumpPower end
    end)
    task.spawn(function()
        while task.wait(1) do
            if _G.KillerEspActive then
                local folder = workspace:FindFirstChild("Killers")
                if folder then for _, k in pairs(folder:GetChildren()) do ApplyESP(k, Color3.new(1, 0, 0), false) end end
            end
            if _G.PlayerEspActive then
                for _, p in pairs(Players:GetPlayers()) do if p ~= LocalPlayer and p.Character then ApplyESP(p.Character, Color3.new(0, 1, 1), true) end end
            end
        end
    end)
end
